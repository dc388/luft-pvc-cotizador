/** Identificador inyectado por Vite en cada build; ver vite.config.ts. */
declare const __LUFT_BUILD_ID__: string;
/** `true` solo bajo el servidor de desarrollo (`vite`); cualquier build lo deja en `false`. */
declare const __LUFT_LOCAL_DEV__: boolean;
